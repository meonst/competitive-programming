#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    ios_base ::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int N;
    cin >> N;
    vector<vector<ll>> stair_number(N, vector<ll>(10, 0));
    for (int i = 2; i < 101; i++)
    {

    }
}
