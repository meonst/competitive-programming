#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    ios_base ::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll mod;
    
    int T;
    cin >> T;
    vector<vector<ll>> cases(501, vector<ll>(4501, 0));

    for (int i = 0; i < 10; i++)
        cases[1][i] = 1;
        




    for (int i = 0; i < T; i++)
    {

    }
}
