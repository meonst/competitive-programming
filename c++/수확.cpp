#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

vector<vector<int>> max_profit(2001, vector<int>(2001));
vector<int> farm(2001);

int solve(int start, int end, int reaped)
{

}

int main()
{
    ios_base ::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int N;
    cin >> N;
    
    for (int i = 1; i <= N; i++)
    {
        cin >> farm[i];
    }
    
    solve()

    cout << sum;
}
